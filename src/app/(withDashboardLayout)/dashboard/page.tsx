import { Typography } from "@mui/material";

const page = () => {
  return (
    <div>
      <Typography variant="h1" component="h1">
        Dashboard Page
      </Typography>
    </div>
  );
};

export default page;
